import React from 'react';
import * as Styled from './Styled.App';
import Layout from 'layout/Layout';

const App = () => {
  return (
    <Styled.App>
      <Layout />
    </Styled.App>
  );
};

export default App;

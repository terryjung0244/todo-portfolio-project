import React from 'react';
import * as Styled from './CreateTodo.Styled';

const CreateTodo = () => {
  return (
    <Styled.CreateTodo>
      <div>CreateTodo</div>
    </Styled.CreateTodo>
  );
};

export default CreateTodo;

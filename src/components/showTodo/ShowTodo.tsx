import React from 'react';
import * as Styled from './ShowTodo.Styled';

const ShowTodo = () => {
  return (
    <Styled.ShowTodo>
      <div>ShowTodo</div>
    </Styled.ShowTodo>
  );
};

export default ShowTodo;
